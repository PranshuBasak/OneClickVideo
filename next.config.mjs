/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['picsum.photos','firebasestorage.googleapis.com'], // Add any other image domains you need here
    },
  };
  
  export default nextConfig;
  